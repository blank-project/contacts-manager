<ul>
  {{#each contacts}}
    <li><a href="/contacts/duplicates/{{ this.ids }}">
      Il y a {{ this.count }} comptes avec pour nom :
      {{#each this._id.name}}
        {{ this }}
      {{/each}}
    </a></li>
  {{/each}}
</ul>